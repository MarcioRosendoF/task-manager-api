spring.application.name=taskmanager
# Habilita o console visual do banco (acesse em /h2-console)
spring.h2.console.enabled=true

# Nome do banco em memoria
spring.datasource.url=jdbc:h2:mem:taskdb

# Mostra as queries SQL no console — util para entender o que esta acontecendo
spring.jpa.show-sql=true

